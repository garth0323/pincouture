<hr/>
<p><%= link_to "Add Celebrity", new_celebrity_path, class: "btn btn-primary"%></p>

<div class="list-group">
  <a href="#" class="list-group-item active">
    Celebrities
  </a>
  <% @celebrities.each do |celebrity| %>
  	<%= link_to celebrity.name, celebrity_path(celebrity), class: "list-group-item" %>
  <% end %>
</div>

<%= will_paginate @posts %>